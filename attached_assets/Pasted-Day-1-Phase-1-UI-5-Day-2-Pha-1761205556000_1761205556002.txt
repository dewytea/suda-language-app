Day 1 (오늘): Phase 1 - 기본 구조 + 지문 표시
├─ 데이터베이스 설계
├─ 지문 표시 UI
├─ 난이도 필터
└─ 샘플 지문 5개

Day 2: Phase 2 - 독해 문제 + 채점
├─ 문제 시스템
├─ 답안 제출
├─ 점수 계산
└─ 결과 표시

Day 3: Phase 3 - 읽기 속도 + 통계
├─ 타이머 기능
├─ WPM 계산
├─ 통계 페이지
└─ 학습 기록

Day 4: Phase 4 - 콘텐츠 추가 + 마무리
├─ 지문 20-30개 추가
├─ 문제 작성
├─ 버그 수정
└─ 최종 테스트
```

---

## 🚀 **Phase 1: 기본 구조 + 지문 표시 (지금 시작!)**

### ⏰ **예상 소요 시간: 3-4시간**

---

## 📝 **Replit Agent에 바로 붙여넣으세요!**
```
Suda 앱에 Reading (읽기/독해) 모듈을 새로 추가해줘.

=== Reading 모듈: Phase 1 - 기본 구조 + 지문 표시 ===

Reading은 영어 지문을 읽고 독해 문제를 푸는 모듈이야.
이번 단계에서는 기본 구조와 지문 표시를 만들 거야.

=== 데이터베이스 테이블 생성 ===

1. reading_passages 테이블 (지문):
```sql
CREATE TABLE reading_passages (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  content_type TEXT NOT NULL, -- 'news', 'story', 'essay', 'email', 'ad'
  difficulty INTEGER NOT NULL CHECK (difficulty BETWEEN 1 AND 5),
  word_count INTEGER NOT NULL,
  estimated_time INTEGER, -- 예상 읽기 시간 (초)
  source TEXT, -- 출처
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_reading_passages_difficulty ON reading_passages(difficulty);
CREATE INDEX idx_reading_passages_content_type ON reading_passages(content_type);

-- 샘플 지문 추가 (5개)

-- Level 1: 초급 - 자기소개
INSERT INTO reading_passages (title, content, content_type, difficulty, word_count, estimated_time) VALUES (
  'My Daily Routine',
  'My name is Sarah. I am a student. I wake up at 7 AM every day. I eat breakfast with my family. Then I go to school by bus. School starts at 8:30 AM. I have lunch at 12 PM. After school, I do my homework. I like to read books in the evening. I go to bed at 10 PM.',
  'story',
  1,
  68,
  40
);

-- Level 2: 초급 - 간단한 이메일
INSERT INTO reading_passages (title, content, content_type, difficulty, word_count, estimated_time) VALUES (
  'Party Invitation',
  'Dear friends,

I am having a birthday party next Saturday at 3 PM. The party will be at my house. There will be cake, games, and music. Please bring your friends too! Let me know if you can come.

See you there!
Emma',
  'email',
  2,
  52,
  35
);

-- Level 3: 중급 - 뉴스 기사
INSERT INTO reading_passages (title, content, content_type, difficulty, word_count, estimated_time) VALUES (
  'New Park Opens Downtown',
  'The city opened a new park downtown yesterday. The park has walking paths, playgrounds, and a small lake. Mayor Johnson said the park will help families enjoy outdoor activities. "We wanted to create a beautiful space for everyone," the mayor explained. The park took two years to build and cost 5 million dollars. Local residents are excited about the new addition to their neighborhood. The park is open daily from 6 AM to 9 PM.',
  'news',
  3,
  85,
  50
);

-- Level 4: 중상급 - 에세이
INSERT INTO reading_passages (title, content, content_type, difficulty, word_count, estimated_time) VALUES (
  'The Benefits of Reading',
  'Reading is one of the most valuable habits a person can develop. It expands our knowledge, improves our vocabulary, and stimulates our imagination. When we read regularly, we expose ourselves to different perspectives and ideas. This helps us understand the world better and makes us more empathetic toward others.

Research shows that reading can reduce stress levels significantly. Just six minutes of reading can lower stress by up to 68 percent, according to scientists at the University of Sussex. Reading before bed can also improve sleep quality.

Furthermore, reading keeps our minds active and engaged. Like physical exercise strengthens the body, mental exercise through reading strengthens the brain. Studies suggest that regular reading may even help prevent cognitive decline as we age.',
  'essay',
  4,
  128,
  75
);

-- Level 5: 고급 - 과학 기사
INSERT INTO reading_passages (title, content, content_type, difficulty, word_count, estimated_time) VALUES (
  'Artificial Intelligence in Healthcare',
  'Artificial intelligence is revolutionizing the healthcare industry in unprecedented ways. Machine learning algorithms can now detect diseases from medical images with accuracy that rivals or exceeds human experts. For instance, AI systems have demonstrated remarkable success in identifying early-stage cancers, cardiovascular conditions, and neurological disorders.

The implementation of AI in healthcare extends beyond diagnosis. Predictive analytics help hospitals optimize resource allocation, reducing wait times and improving patient outcomes. Natural language processing enables automated analysis of medical records, identifying patterns that might escape human observation. Robot-assisted surgeries allow for greater precision, resulting in fewer complications and faster recovery times.

However, the integration of AI into healthcare raises important ethical questions. Issues of data privacy, algorithmic bias, and the role of human judgment in medical decision-making require careful consideration. As this technology continues to evolve, establishing appropriate regulatory frameworks and ethical guidelines becomes increasingly crucial to ensure that AI serves the best interests of patients while maintaining the fundamental principles of medical practice.',
  'news',
  5,
  168,
  100
);
```

2. reading_questions 테이블 (독해 문제):
```sql
CREATE TABLE reading_questions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  passage_id UUID REFERENCES reading_passages(id) ON DELETE CASCADE,
  question_text TEXT NOT NULL,
  question_type TEXT NOT NULL, -- 'main_idea', 'detail', 'inference', 'vocabulary'
  options JSONB NOT NULL, -- 객관식 선택지 배열
  correct_answer TEXT NOT NULL,
  explanation TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_reading_questions_passage_id ON reading_questions(passage_id);

-- 각 지문에 문제 3-4개씩 추가 (나중에 Phase 2에서 추가)
```

3. reading_progress 테이블 (학습 진도):
```sql
CREATE TABLE reading_progress (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES profiles(id) ON DELETE CASCADE,
  passage_id UUID REFERENCES reading_passages(id) ON DELETE CASCADE,
  answers JSONB, -- 사용자 답변들
  score INTEGER CHECK (score BETWEEN 0 AND 100),
  correct_count INTEGER,
  total_count INTEGER,
  reading_time INTEGER, -- 읽는데 걸린 시간 (초)
  wpm FLOAT, -- Words Per Minute
  completed BOOLEAN DEFAULT false,
  completed_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_reading_progress_user_id ON reading_progress(user_id);
CREATE INDEX idx_reading_progress_passage_id ON reading_progress(passage_id);
```

=== 메인 페이지 ===

app/learn/reading/page.tsx 생성:
```tsx
'use client'

import { useEffect, useState } from 'react'
import { BookOpen, Clock, FileText, TrendingUp } from 'lucide-react'
import Link from 'next/link'

interface Passage {
  id: string
  title: string
  content: string
  content_type: string
  difficulty: number
  word_count: number
  estimated_time: number
}

const contentTypeIcons: Record<string, string> = {
  news: '📰',
  story: '📖',
  essay: '✍️',
  email: '✉️',
  ad: '📢'
}

const contentTypeNames: Record<string, string> = {
  news: '뉴스',
  story: '이야기',
  essay: '에세이',
  email: '이메일',
  ad: '광고'
}

export default function ReadingPage() {
  const [passages, setPassages] = useState<Passage[]>([])
  const [loading, setLoading] = useState(true)
  const [selectedDifficulty, setSelectedDifficulty] = useState<number | null>(null)
  const [selectedType, setSelectedType] = useState<string | null>(null)
  
  useEffect(() => {
    fetchPassages()
  }, [selectedDifficulty, selectedType])
  
  const fetchPassages = async () => {
    try {
      const params = new URLSearchParams()
      if (selectedDifficulty) params.append('difficulty', selectedDifficulty.toString())
      if (selectedType) params.append('type', selectedType)
      
      const response = await fetch(`/api/reading/passages?${params}`)
      const data = await response.json()
      setPassages(data.passages || [])
    } catch (error) {
      console.error('Failed to fetch passages:', error)
    } finally {
      setLoading(false)
    }
  }
  
  const contentTypes = ['news', 'story', 'essay', 'email']
  const difficulties = [1, 2, 3, 4, 5]
  
  return (
    <div className="max-w-6xl mx-auto p-6">
      {/* 헤더 */}
      <div className="mb-8">
        <div className="flex items-center gap-3 mb-3">
          <div className="w-12 h-12 bg-gradient-to-br from-purple-500 to-pink-500 rounded-xl flex items-center justify-center">
            <BookOpen className="w-6 h-6 text-white" />
          </div>
          <div>
            <h1 className="text-3xl font-bold text-gray-900">Reading</h1>
            <p className="text-gray-600">영어 지문을 읽고 독해력을 키워요</p>
          </div>
        </div>
      </div>
      
      {/* 통계 카드 (간단) */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
        <Link
          href="/learn/reading/history"
          className="bg-white rounded-xl border border-gray-200 p-4 hover:border-purple-300 hover:shadow-md transition-all"
        >
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-gray-600">학습 기록</p>
              <p className="text-2xl font-bold text-gray-900">-</p>
            </div>
            <FileText className="w-8 h-8 text-purple-600" />
          </div>
        </Link>
        
        <Link
          href="/learn/reading/stats"
          className="bg-white rounded-xl border border-gray-200 p-4 hover:border-purple-300 hover:shadow-md transition-all"
        >
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-gray-600">통계</p>
              <p className="text-2xl font-bold text-gray-900">-</p>
            </div>
            <TrendingUp className="w-8 h-8 text-purple-600" />
          </div>
        </Link>
        
        <div className="bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl border border-purple-200 p-4">
          <div className="flex items-center justify-between">
            <div>
              <p className="text-sm text-purple-900 font-medium">평균 읽기 속도</p>
              <p className="text-2xl font-bold text-purple-900">- WPM</p>
            </div>
            <Clock className="w-8 h-8 text-purple-600" />
          </div>
        </div>
      </div>
      
      {/* 필터 */}
      <div className="bg-white rounded-xl border border-gray-200 p-6 mb-6">
        <div className="space-y-4">
          {/* 난이도 필터 */}
          <div>
            <h3 className="text-sm font-semibold text-gray-700 mb-3">난이도</h3>
            <div className="flex flex-wrap gap-2">
              <button
                onClick={() => setSelectedDifficulty(null)}
                className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
                  selectedDifficulty === null
                    ? 'bg-purple-600 text-white'
                    : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
                }`}
              >
                전체
              </button>
              {difficulties.map((diff) => (
                <button
                  key={diff}
                  onClick={() => setSelectedDifficulty(diff)}
                  className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
                    selectedDifficulty === diff
                      ? 'bg-purple-600 text-white'
                      : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
                  }`}
                >
                  Level {diff}
                </button>
              ))}
            </div>
          </div>
          
          {/* 유형 필터 */}
          <div>
            <h3 className="text-sm font-semibold text-gray-700 mb-3">유형</h3>
            <div className="flex flex-wrap gap-2">
              <button
                onClick={() => setSelectedType(null)}
                className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
                  selectedType === null
                    ? 'bg-purple-600 text-white'
                    : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
                }`}
              >
                전체
              </button>
              {contentTypes.map((type) => (
                <button
                  key={type}
                  onClick={() => setSelectedType(type)}
                  className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
                    selectedType === type
                      ? 'bg-purple-600 text-white'
                      : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
                  }`}
                >
                  {contentTypeIcons[type]} {contentTypeNames[type]}
                </button>
              ))}
            </div>
          </div>
        </div>
      </div>
      
      {/* 지문 리스트 */}
      {loading ? (
        <div className="flex justify-center py-12">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-purple-600" />
        </div>
      ) : passages.length === 0 ? (
        <div className="text-center py-12">
          <BookOpen className="w-16 h-16 text-gray-300 mx-auto mb-4" />
          <p className="text-gray-600">선택한 조건의 지문이 없어요</p>
        </div>
      ) : (
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {passages.map((passage) => (
            <Link
              key={passage.id}
              href={`/learn/reading/${passage.id}`}
              className="bg-white rounded-xl border border-gray-200 p-5 hover:border-purple-300 hover:shadow-lg transition-all"
            >
              <div className="flex items-start justify-between mb-3">
                <div className="flex items-center gap-2">
                  <span className="text-3xl">
                    {contentTypeIcons[passage.content_type]}
                  </span>
                  <div>
                    <span className="text-xs font-medium text-gray-500">
                      Level {passage.difficulty}
                    </span>
                    <p className="text-xs text-gray-400">
                      {contentTypeNames[passage.content_type]}
                    </p>
                  </div>
                </div>
              </div>
              
              <h3 className="text-lg font-semibold text-gray-900 mb-3 line-clamp-2">
                {passage.title}
              </h3>
              
              <p className="text-sm text-gray-600 mb-4 line-clamp-3">
                {passage.content}
              </p>
              
              <div className="flex items-center gap-4 text-xs text-gray-500">
                <span className="flex items-center gap-1">
                  <FileText className="w-3 h-3" />
                  {passage.word_count} 단어
                </span>
                <span className="flex items-center gap-1">
                  <Clock className="w-3 h-3" />
                  약 {Math.ceil(passage.estimated_time / 60)}분
                </span>
              </div>
            </Link>
          ))}
        </div>
      )}
    </div>
  )
}
```

=== 지문 읽기 페이지 (기본) ===

app/learn/reading/[id]/page.tsx 생성:
```tsx
'use client'

import { useEffect, useState } from 'react'
import { useParams, useRouter } from 'next/navigation'
import { ArrowLeft, Clock, BookOpen } from 'lucide-react'
import Link from 'next/link'

interface Passage {
  id: string
  title: string
  content: string
  content_type: string
  difficulty: number
  word_count: number
  estimated_time: number
}

export default function ReadingPassagePage() {
  const params = useParams()
  const router = useRouter()
  const [passage, setPassage] = useState<Passage | null>(null)
  const [loading, setLoading] = useState(true)
  const [startTime, setStartTime] = useState<Date | null>(null)
  const [showQuestions, setShowQuestions] = useState(false)
  
  useEffect(() => {
    if (params.id) {
      fetchPassage(params.id as string)
    }
  }, [params.id])
  
  useEffect(() => {
    // 읽기 시작 시간 기록
    if (passage && !startTime) {
      setStartTime(new Date())
    }
  }, [passage])
  
  const fetchPassage = async (id: string) => {
    try {
      const response = await fetch(`/api/reading/passages/${id}`)
      const data = await response.json()
      setPassage(data.passage)
    } catch (error) {
      console.error('Failed to fetch passage:', error)
    } finally {
      setLoading(false)
    }
  }
  
  const handleStartQuestions = () => {
    setShowQuestions(true)
    // Phase 2에서 독해 문제 컴포넌트로 이동
  }
  
  if (loading) {
    return (
      <div className="flex items-center justify-center h-screen">
        <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-purple-600" />
      </div>
    )
  }
  
  if (!passage) {
    return (
      <div className="flex flex-col items-center justify-center h-screen">
        <BookOpen className="w-16 h-16 text-gray-300 mb-4" />
        <p className="text-gray-600 mb-4">지문을 찾을 수 없어요</p>
        <Link
          href="/learn/reading"
          className="px-6 py-3 bg-purple-600 text-white rounded-lg hover:bg-purple-700"
        >
          목록으로 돌아가기
        </Link>
      </div>
    )
  }
  
  return (
    <div className="max-w-4xl mx-auto p-6">
      {/* 헤더 */}
      <div className="mb-6">
        <Link
          href="/learn/reading"
          className="inline-flex items-center gap-2 text-purple-600 hover:text-purple-700 mb-4"
        >
          <ArrowLeft className="w-4 h-4" />
          목록으로
        </Link>
        
        <div className="bg-white rounded-xl border border-gray-200 p-6">
          <div className="flex items-start justify-between mb-4">
            <div>
              <h1 className="text-2xl font-bold text-gray-900 mb-2">
                {passage.title}
              </h1>
              <div className="flex items-center gap-4 text-sm text-gray-600">
                <span>Level {passage.difficulty}</span>
                <span>•</span>
                <span className="flex items-center gap-1">
                  <BookOpen className="w-4 h-4" />
                  {passage.word_count} 단어
                </span>
                <span>•</span>
                <span className="flex items-center gap-1">
                  <Clock className="w-4 h-4" />
                  약 {Math.ceil(passage.estimated_time / 60)}분
                </span>
              </div>
            </div>
          </div>
          
          {/* 안내 메시지 */}
          <div className="bg-purple-50 border border-purple-200 rounded-lg p-4">
            <p className="text-sm text-purple-900">
              💡 <strong>팁:</strong> 천천히 읽으며 내용을 이해하세요. 
              다 읽으면 독해 문제를 풀 수 있어요.
            </p>
          </div>
        </div>
      </div>
      
      {/* 지문 내용 */}
      <div className="bg-white rounded-xl border border-gray-200 p-8 mb-6">
        <div className="prose prose-lg max-w-none">
          {passage.content.split('\n\n').map((paragraph, index) => (
            <p key={index} className="mb-4 leading-relaxed text-gray-800">
              {paragraph}
            </p>
          ))}
        </div>
      </div>
      
      {/* 독해 문제 시작 버튼 */}
      <div className="flex justify-center">
        <button
          onClick={handleStartQuestions}
          className="px-8 py-4 bg-purple-600 text-white rounded-xl hover:bg-purple-700 font-semibold text-lg shadow-lg hover:shadow-xl transition-all"
        >
          독해 문제 풀기
        </button>
      </div>
      
      {/* Phase 2에서 독해 문제 UI 추가 예정 */}
      {showQuestions && (
        <div className="mt-6 bg-yellow-50 border border-yellow-200 rounded-xl p-6 text-center">
          <p className="text-yellow-800">
            독해 문제는 Phase 2에서 추가될 예정입니다.
          </p>
        </div>
      )}
    </div>
  )
}
```

=== API 엔드포인트 ===

1. app/api/reading/passages/route.ts (지문 목록):
```tsx
import { createRouteHandlerClient } from '@supabase/auth-helpers-nextjs'
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'

export async function GET(req: Request) {
  try {
    const { searchParams } = new URL(req.url)
    const difficulty = searchParams.get('difficulty')
    const type = searchParams.get('type')
    
    const supabase = createRouteHandlerClient({ cookies })
    
    let query = supabase
      .from('reading_passages')
      .select('*')
      .order('difficulty', { ascending: true })
    
    if (difficulty) {
      query = query.eq('difficulty', parseInt(difficulty))
    }
    
    if (type) {
      query = query.eq('content_type', type)
    }
    
    const { data, error } = await query
    
    if (error) throw error
    
    return NextResponse.json({ passages: data })
  } catch (error) {
    console.error('Passages fetch error:', error)
    return NextResponse.json(
      { error: 'Failed to fetch passages' },
      { status: 500 }
    )
  }
}
```

2. app/api/reading/passages/[id]/route.ts (단일 지문):
```tsx
import { createRouteHandlerClient } from '@supabase/auth-helpers-nextjs'
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'

export async function GET(
  req: Request,
  { params }: { params: { id: string } }
) {
  try {
    const supabase = createRouteHandlerClient({ cookies })
    
    const { data, error } = await supabase
      .from('reading_passages')
      .select('*')
      .eq('id', params.id)
      .single()
    
    if (error) throw error
    
    return NextResponse.json({ passage: data })
  } catch (error) {
    console.error('Passage fetch error:', error)
    return NextResponse.json(
      { error: 'Failed to fetch passage' },
      { status: 500 }
    )
  }
}
```

=== 사이드바 메뉴 업데이트 ===

기존 사이드바에 Reading 메뉴 추가:

Reading 메뉴:
- 아이콘: 📚 또는 <BookOpen />
- 링크: /learn/reading
- 하위 메뉴:
  ├─ 지문 읽기 (/learn/reading)
  ├─ 학습 기록 (/learn/reading/history)
  └─ 통계 (/learn/reading/stats)

=== 출력 요구사항 ===

1. ✅ reading_passages 테이블 생성
2. ✅ reading_questions 테이블 생성
3. ✅ reading_progress 테이블 생성
4. ✅ 샘플 지문 5개 추가 (Level 1-5)
5. ✅ 메인 페이지 (/learn/reading)
6. ✅ 난이도 필터 (1-5)
7. ✅ 유형 필터 (뉴스/이야기/에세이/이메일)
8. ✅ 지문 카드 UI
9. ✅ 지문 읽기 페이지 (/learn/reading/[id])
10. ✅ 지문 내용 표시
11. ✅ 읽기 시작 시간 기록
12. ✅ "독해 문제 풀기" 버튼
13. ✅ API 엔드포인트 2개
14. ✅ 사이드바 메뉴 추가
15. ✅ 모바일 반응형

완성 후:
- Reading Phase 1 완료!
- 기본 구조 완성
- 지문 표시 작동
- 내일 Phase 2 (독해 문제) 진행

테스트:
1. /learn/reading 접속
2. 필터 작동 확인
3. 지문 카드 클릭
4. 지문 내용 표시 확인
5. 읽기 편한지 확인
6. 모바일에서 확인

지금 바로 만들어줘!
완성되면 알려줘!
```

---

## ⏰ **예상 소요 시간**

- Replit Agent 구현: 3-4시간

---

## 🎉 **완성되면**

**Reading Phase 1 완료!**
```
✅ 기본 구조
✅ 데이터베이스
✅ 지문 표시
✅ 필터링

다음 (Phase 2):
- 독해 문제
- 답안 제출
- 점수 계산

→ 내일 계속!
```

---

## 📊 **현재 진행 상황**
```
완성:
├─ Speaking ✅
├─ AI Chat ✅
├─ Gamification ✅
└─ Reading Phase 1 ← 진행중!

보류 (나중에):
├─ Listening Phase 2
└─ 단어 팝업

다음:
├─ Reading Phase 2-4
├─ Writing 또는 Vocabulary
└─ 최종 완성!